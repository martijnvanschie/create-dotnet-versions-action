name: Verify Create Dotnet Version

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  create-dotnet-versions-local:
    runs-on: ubuntu-latest
    name: Test the create-dotnet-versions locally
    steps:
      - name: Checkout
        uses: actions/checkout@v3    

      - name: Create version step
        id: dotnetversion
        uses: ./
        with:
          baseversion: 'v1.2.3-beta.4'

      - name: Echo all version outputs
        shell: bash
        run: |
          echo 'version-assembly = ${{ steps.dotnetversions.outputs.version-assembly }}'
          echo "version-file = ${{ steps.dotnetversions.outputs.version-file }}"
          echo "version-informational = ${{ steps.dotnetversions.outputs.version-informational }}"
          echo "version-package = ${{ steps.dotnetversions.outputs.version-package }}"
          echo "buildnumber = ${{ steps.dotnetversions.outputs.buildnumber }}"